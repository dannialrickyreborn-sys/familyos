# Capability Registry

This registry tracks every capability FamilyOS has ever proposed, regardless of its current lifecycle state (see `docs/capabilities/CAPABILITY-LIFECYCLE.md`). Each row corresponds to a detailed capability document written from `docs/capabilities/templates/CAPABILITY_TEMPLATE.md`.

| Capability ID | Name | Status | Problem Solved | Inputs | Outputs | Dependencies | Validation Date | Owner | Notes |
|---|---|---|---|---|---|---|---|---|---|
| CAP-001 | Daily Brief | Testing | No single place to see today's tasks, events, bills, and documents at a glance | Notion Tasks, Calendar, Bills, Documents databases | Terminal-printed executive brief (`familyos brief`) | `familyos` CLI, Notion integration token | | Repository owner | Implemented; not yet validated through sustained daily use. See `CAP-001-daily-brief.md`. |
| CAP-002 | WhatsApp Foundation | Active | No way to deliver FamilyOS output to the family's phones without a paid or Meta-hosted API | Personal WhatsApp account (paired once), `WHATSAPP_TARGET` | Messages delivered to a personal WhatsApp account; `whatsapp:link` / `whatsapp:status` | `familyos` CLI, `@whiskeysockets/baileys` 6.7.23 | 2026-08-05 | Repository owner | Validated end-to-end on a real device: pairing code succeeded, session stored consistently, `whatsapp:status` reports linked and connected. See `whatsapp-foundation.md`. |
| CAP-003 | Family Registry | Active | Identity had no home; a phone number was the only thing FamilyOS knew about a person | `configs/family.json` (id, name, phone, role, active) | Validated member records; `familyos family` | `familyos` CLI | 2026-08-05 | Repository owner | Single source of truth for phone numbers. See `family-registry.md`. |
| CAP-004 | Message Router | Active | No way to tell family from strangers, or to act on what they send | Inbound message (sender JID or phone, text) + Family Registry | Routing decision and command reply; `familyos message` | CAP-003, `familyos` CLI | 2026-08-05 | Repository owner | Routing and `/help` `/status` `/family` `/ping` complete and tested; inbound WhatsApp delivery not yet wired. See `message-router.md`. |
| CAP-005 | Capability Runtime | Active | Commands were dispatched by a static switch, so every new command meant editing the router | Parsed command + resolved member + family registry | Capability response (reply, permission refusal, or unknown command); `/help` `/status` `/family` `/ping` | CAP-003, CAP-004 | 2026-08-05 | Repository owner | Pluggable: adding a capability is one new module in `src/capabilities/`. See `capability-runtime.md`. |
| CAP-006 | WhatsApp Inbound | Active | Commands were only reachable from the CLI; a family member could not just message FamilyOS | WhatsApp `messages.upsert` events | Automatic replies to registered members; `familyos listen` | CAP-002, CAP-004, CAP-005 | | Repository owner | Full chain tested against a stand-in socket; live round trip needs confirming on a device. See `whatsapp-inbound.md`. |
